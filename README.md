# 微掌柜 - 打单发货管理系统

## 📋 项目概述

本项目是一个完整的物流订单管理系统，采用前端技术栈实现了订单创建、列表管理、数据统计和可视化等核心功能。系统使用LocalStorage作为数据持久化方案，无需后端支持即可完整运行，非常适合作为Web前端课程的综合实践项目。

### 项目特点
- ✅ **完整的业务流程**：从订单创建到数据展示的完整闭环
- ✅ **数据持久化**：基于LocalStorage的本地数据库实现
- ✅ **实时数据更新**：创建、删除、修改订单后实时刷新统计和图表
- ✅ **现代化UI**：简洁美观的界面设计，良好的用户体验
- ✅ **响应式布局**：支持桌面端和移动端访问
- ✅ **纯原生开发**：不依赖任何第三方框架和库

---

## 🎯 核心功能

### 1. 订单管理

#### 创建订单 📝
- **自动生成订单号**：格式为 `WZG + 年月日时分秒 + 随机数`，确保唯一性
- **支持手动修改**：用户可以自定义订单号
- **完整信息录入**：
  - 收货人姓名、联系电话（必填）
  - 详细收货地址（必填）
  - 商品名称、数量、单价（必填）
  - 快递公司选择（顺丰、圆通、中通、申通、韵达、EMS）
  - 订单备注信息
- **表单验证**：必填项检查，确保数据完整性
- **实时保存**：订单创建后立即保存到LocalStorage

#### 订单列表 📋
- **数据展示**：表格形式展示所有订单信息
- **搜索功能**：支持按订单号、收货人姓名、联系电话、商品名称搜索
- **状态筛选**：可按状态筛选订单
  - 未支付
  - 未打印
  - 未发货
  - 运输中
  - 已完成
  - 已取消
- **订单操作**：
  - 查看详情：弹窗显示订单完整信息
  - 打印订单：更新订单状态为"未发货"
  - 删除订单：从数据库中移除订单
- **空数据提示**：无订单时显示友好提示

### 2. 数据统计

#### 实时统计面板
- **今日统计**：
  - 未打印订单数
  - 未发货订单数
  - 预打印订单数
  - 已打印订单数

- **近7日统计**：
  - 运输中订单数
  - 已签收订单数
  - 已取消订单数

- **自动更新**：创建、删除、修改订单后自动刷新统计数据

### 3. 数据可视化

#### 订单走势图表 📊
- **Canvas绘制**：原生Canvas实现的折线图
- **7天数据**：展示最近7天的订单数量趋势
- **实时数据**：从LocalStorage读取真实订单数据
- **日期标注**：X轴显示日期，Y轴显示订单数量
- **自适应缩放**：根据数据范围自动调整Y轴刻度
- **视觉效果**：渐变填充、平滑曲线、数据点标记

---

## 🎨 界面设计

### 布局结构
- **左侧导航栏**：固定侧边栏，包含Logo和导航菜单
- **主内容区域**：自适应宽度，显示统计、图表和功能入口
- **模态框设计**：弹出式窗口用于订单创建和列表展示

### 设计风格
- **配色方案**：
  - 主色调：蓝色 (#4D9BFF)
  - 辅助色：橙色 (#FF9448)、绿色 (#5FC88F)
  - 背景色：浅灰 (#f5f7fa)
- **图标使用**：Emoji图标，简洁直观
- **卡片设计**：圆角卡片，阴影效果
- **动画效果**：淡入动画、悬停效果、平滑过渡

### 用户体验优化
- **表单优化**：清晰的标签、必填项标识、输入提示
- **状态标签**：彩色状态标签，快速识别订单状态
- **操作反馈**：按钮悬停效果、点击反馈、成功提示
- **响应式设计**：适配不同屏幕尺寸

---

## 💻 技术实现

### 技术栈
- **HTML5**：语义化标签，表单元素
- **CSS3**：
  - Flexbox/Grid布局
  - 渐变、阴影、圆角
  - 动画和过渡效果
  - 媒体查询（响应式）
- **JavaScript ES6+**：
  - 事件处理
  - DOM操作
  - Canvas绘图
  - LocalStorage API
  - 日期处理
  - 数组方法（filter、map、find等）

### 数据持久化方案

#### LocalStorage作为数据库
```javascript
// 数据结构
{
  orderNumber: "WZG20251226142524085",  // 订单号
  customerName: "张三",                  // 收货人
  customerPhone: "13800138000",         // 电话
  customerAddress: "北京市朝阳区...",    // 地址
  productName: "商品名称",               // 商品
  quantity: 2,                          // 数量
  price: 99.00,                         // 单价
  totalAmount: 198.00,                  // 总金额
  expressCompany: "顺丰速运",           // 快递公司
  remark: "备注信息",                    // 备注
  status: "unprinted",                  // 状态
  createTime: "2025/12/26 14:25:24"    // 创建时间
}
```

#### 数据操作
- **保存**：`localStorage.setItem('orders', JSON.stringify(orders))`
- **读取**：`JSON.parse(localStorage.getItem('orders')) || []`
- **更新**：修改数组后重新保存
- **删除**：过滤数组后重新保存

### 核心算法

#### 订单号生成算法
```javascript
// 格式：WZG + 年月日时分秒 + 3位随机数
// 示例：WZG20251226142524085
const orderNumber = `WZG${year}${month}${day}${hours}${minutes}${seconds}${random}`;
```

#### 日期统计算法
```javascript
// 统计指定日期范围内的订单
const todayOrders = orders.filter(order => {
  const orderDate = new Date(order.createTime);
  orderDate.setHours(0, 0, 0, 0);
  return orderDate.getTime() === today.getTime();
});
```

#### 图表数据处理
```javascript
// 生成7天每天的订单数量数组
for (let i = days - 1; i >= 0; i--) {
  const targetDate = new Date();
  targetDate.setDate(today.getDate() - i);
  const count = orders.filter(/* 匹配该日期 */).length;
  data.push(count);
}
```

---

## 📁 项目结构

```
课程大作业/
├── index.html          # 主页面
├── about.html          # 关于页面
├── README.md           # 项目文档
├── css/
│   └── style.css       # 全局样式
├── js/
│   ├── main.js         # 主要业务逻辑
│   └── charts.js       # 图表绘制
└── images/             # 图片资源
```

### 文件说明

#### index.html
- 页面结构定义
- 侧边栏导航
- 统计面板
- 图表容器
- 功能入口
- 创建订单模态框
- 订单列表模态框

#### style.css
- 全局样式重置
- 布局样式（侧边栏、主内容区）
- 组件样式（按钮、卡片、表格、表单）
- 模态框样式
- 响应式媒体查询
- 动画效果

#### main.js
- 页面初始化
- 事件绑定
- 订单CRUD操作（创建、读取、更新、删除）
- 订单号生成
- 表单处理
- 搜索和筛选
- 模态框控制

#### charts.js
- Canvas图表绘制
- 数据统计计算
- 图表刷新
- 坐标轴绘制
- 数据点标记

---

## 🚀 使用说明

### 运行项目
1. 下载项目文件到本地
2. 使用浏览器直接打开 `index.html`
3. 无需服务器环境，即可完整运行

### 功能操作

#### 创建订单
1. 点击"创建订单"功能图标
2. 填写订单信息（带*为必填项）
3. 点击"创建订单"按钮保存
4. 系统自动刷新统计数据和图表

#### 查看订单列表
1. 点击"订单列表"功能图标
2. 查看所有订单信息
3. 使用搜索框和状态筛选器过滤订单
4. 对订单进行查看、打印、删除操作

#### 查看数据统计
- 主页面直接显示今日和近7日统计
- 订单创建后自动更新数字
- 查看7日订单走势图

### 数据管理

#### 清空所有数据
在浏览器控制台执行：
```javascript
localStorage.removeItem('orders');
location.reload();
```

#### 导出数据
```javascript
const orders = localStorage.getItem('orders');
console.log(orders); // 复制数据进行备份
```

#### 导入数据
```javascript
const data = '[]'; // 粘贴备份的数据
localStorage.setItem('orders', data);
location.reload();
```


## 📊 项目亮点

1. **完整的业务闭环**：从数据录入到展示的完整流程
2. **真实的数据处理**：基于LocalStorage的实际数据存储
3. **实时数据同步**：增删改操作后立即更新界面
4. **原生技术实现**：不依赖框架，展示原生JS能力
5. **优秀的代码质量**：清晰的注释、模块化的结构
6. **良好的用户体验**：流畅的交互、友好的提示

---

## 📝 开发笔记

### 技术难点与解决方案

#### 1. 日期处理
- **问题**：统计不同时间范围的订单
- **方案**：使用Date对象，统一时间为00:00:00进行比较

#### 2. 数据更新同步
- **问题**：订单操作后需要刷新多个组件
- **方案**：封装refreshChartAndStats函数统一更新

#### 3. Canvas坐标计算
- **问题**：图表坐标系与Canvas坐标系转换
- **方案**：建立padding、chartWidth等变量进行映射

#### 4. LocalStorage限制
- **问题**：存储容量限制（通常5MB）
- **方案**：仅存储必要数据，优化数据结构

---

## 📈 图表技术实现详解

### Canvas折线图绘制原理

#### 1. 技术选型
本项目采用原生HTML5 Canvas API实现图表绘制，而非使用ECharts、Chart.js等第三方图表库。选择原生实现的原因：
- **学习价值**：深入理解Canvas API和图形绘制原理
- **性能优化**：无第三方库依赖，减少文件体积
- **灵活可控**：完全掌控绘制逻辑，便于定制
- **技术展示**：展现纯原生JavaScript能力

#### 2. 数据流程

```
订单数据(LocalStorage) → 按日期统计 → 生成7天数据数组 → Canvas坐标映射 → 图形绘制
```

##### 数据获取与统计
```javascript
function getOrderDataForChart(days) {
    const orders = JSON.parse(localStorage.getItem('orders')) || [];
    const data = [];
    
    // 统计每天的订单数量
    for (let i = days - 1; i >= 0; i--) {
        const targetDate = new Date();
        targetDate.setDate(today.getDate() - i);
        targetDate.setHours(0, 0, 0, 0);  // 归零时分秒
        
        const nextDate = new Date(targetDate);
        nextDate.setDate(nextDate.getDate() + 1);
        
        // 筛选该日期范围内的订单
        const count = orders.filter(order => {
            const orderDate = new Date(order.createTime);
            return orderDate >= targetDate && orderDate < nextDate;
        }).length;
        
        data.push(count);
    }
    return data;  // 返回7个数值的数组
}
```

#### 3. 坐标系统设计

##### 坐标转换逻辑
Canvas采用左上角为原点(0,0)的坐标系，需要转换为传统的笛卡尔坐标系：

```javascript
// 设置绘图区域边距
const padding = 40;  // 为坐标轴标签预留空间
const chartWidth = width - padding * 2;   // 实际绘图宽度
const chartHeight = height - padding * 2;  // 实际绘图高度

// 计算数据范围
const maxValue = Math.max(...data);  // 最大订单数
const minValue = Math.min(...data);  // 最小订单数
const valueRange = maxValue - minValue || 1;  // 值域（防止除0）
```

##### X轴坐标计算
```javascript
// 将数据索引(0-6)映射到Canvas X坐标
const x = padding + (chartWidth / (data.length - 1)) * index;
```

##### Y轴坐标计算
```javascript
// 将数据值映射到Canvas Y坐标（注意Y轴翻转）
const y = padding + chartHeight - ((value - minValue) / valueRange) * chartHeight;
```

#### 4. 绘制步骤

##### 步骤1：绘制网格线
```javascript
ctx.strokeStyle = '#e0e0e0';
ctx.lineWidth = 1;
for (let i = 0; i <= 5; i++) {
    const y = padding + (chartHeight / 5) * i;
    ctx.moveTo(padding, y);
    ctx.lineTo(width - padding, y);
}
ctx.stroke();
```

##### 步骤2：绘制折线
```javascript
ctx.beginPath();
data.forEach((value, index) => {
    const x = padding + (chartWidth / (data.length - 1)) * index;
    const y = padding + chartHeight - ((value - minValue) / valueRange) * chartHeight;
    
    if (index === 0) {
        ctx.moveTo(x, y);  // 起点
    } else {
        ctx.lineTo(x, y);  // 连线
    }
});
ctx.strokeStyle = '#1890ff';
ctx.lineWidth = 2;
ctx.stroke();
```

##### 步骤3：填充区域
```javascript
// 闭合路径形成填充区域
ctx.lineTo(width - padding, padding + chartHeight);  // 右下角
ctx.lineTo(padding, padding + chartHeight);          // 左下角
ctx.closePath();
ctx.fillStyle = 'rgba(24, 144, 255, 0.1)';
ctx.fill();
```

##### 步骤4：绘制数据点
```javascript
ctx.fillStyle = '#1890ff';
data.forEach((value, index) => {
    const x = padding + (chartWidth / (data.length - 1)) * index;
    const y = padding + chartHeight - ((value - minValue) / valueRange) * chartHeight;
    
    ctx.beginPath();
    ctx.arc(x, y, 3, 0, Math.PI * 2);  // 半径3px的圆点
    ctx.fill();
});
```

##### 步骤5：绘制坐标轴标签
```javascript
// X轴日期标签
for (let i = 0; i < 6; i++) {
    const index = Math.floor((data.length - 1) / 5 * i);
    const x = padding + (chartWidth / (data.length - 1)) * index;
    const date = new Date();
    date.setDate(date.getDate() - (6 - index));
    const label = `${date.getMonth() + 1}/${date.getDate()}`;
    ctx.fillText(label, x, height - 15);
}

// Y轴数值标签
for (let i = 0; i <= 5; i++) {
    const value = minValue + (valueRange / 5) * (5 - i);
    const y = padding + (chartHeight / 5) * i;
    ctx.fillText(Math.round(value), padding - 10, y + 5);
}
```

#### 5. 自适应特性

##### 响应式尺寸
```javascript
// Canvas尺寸自适应容器
canvas.width = canvas.offsetWidth;
canvas.height = 300;
```

##### 动态Y轴范围
系统根据实际数据自动计算Y轴范围，确保图表始终以最佳比例显示：
- 数据较小时：Y轴从0开始
- 数据较大时：Y轴从接近最小值开始
- 值域为0时：设置默认范围防止除零错误

#### 6. 实时数据更新

```javascript
function refreshChartAndStats() {
    const canvas = document.getElementById('orderTrendChart');
    if (canvas) {
        const ctx = canvas.getContext('2d');
        drawLineChart(ctx, canvas.width, canvas.height);
    }
    updateStatistics();
}
```

每次订单数据变化时（创建、删除、修改）：
1. 重新从LocalStorage读取订单数据
2. 重新统计每天的订单数量
3. 清空Canvas并重新绘制图表
4. 同步更新统计面板数据

#### 7. 性能优化

- **最小重绘**：仅在数据变化时重绘，而非定时刷新
- **数据缓存**：统计结果直接使用，避免重复计算
- **坐标预计算**：绘制前一次性计算所有坐标点
- **路径优化**：使用单一路径绘制折线，减少beginPath调用

#### 8. 技术要点总结

| 技术点 | 实现方式 | 作用 |
|--------|----------|------|
| 数据统计 | Date对象 + filter方法 | 按日期分组统计订单数量 |
| 坐标映射 | 线性插值算法 | 数据值转Canvas坐标 |
| Y轴翻转 | chartHeight - y | 适配Canvas坐标系 |
| 自适应范围 | Math.max/min动态计算 | 最佳显示比例 |
| 渐变填充 | rgba透明色 | 视觉美化 |
| 圆角数值 | Math.round | 整数显示 |
| 日期格式化 | getMonth()+1/getDate() | 月/日格式 |
| 实时刷新 | localStorage监听 | 数据同步更新 |

这种实现方式完全不依赖任何图表库，展示了对Canvas API的深入理解和运用，是学习Web图形绘制的优秀实践案例。

---

## 👨‍💻 作者信息

- **项目名称**：微掌柜 - 打单发货管理系统
- **开发时间**：2025年12月
- **课程**：Web前端开发
- **技术支持**：HTML5 + CSS3 + JavaScript

---

## 📄 许可证

本项目仅用于学习和教学目的，欢迎学习交流。

---

## 🙏 致谢

感谢所有为Web前端技术发展做出贡献的开发者们！

---

**项目演示地址**：直接打开 index.html 即可体验完整功能

**建议浏览器**：Chrome、Edge、Firefox等现代浏览器

**最佳分辨率**：1920x1080及以上
